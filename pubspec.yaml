name: takizawa_hackathon_vol8
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  
  # 状態管理
  flutter_riverpod: ^2.6.1
  
  # アニメーション
  lottie: ^3.1.2

  # Image picker
  image_picker: ^1.0.4
  
  # Permissions
  permission_handler: ^11.0.1
  
  # Android最適化：段階的機能復元
  device_info_plus: ^9.1.0
  flutter_local_notifications: ^17.2.3

  firebase_core: ^4.1.1
  cloud_firestore: ^6.0.2
  firebase_auth: ^6.1.0
  firebase_storage: ^13.0.2
  
  geolocator: ^11.0.0  # Step 3: 位置情報サービス復元 ✅
  geocoding: ^3.0.0    # Step 4: ジオコーディング機能追加 ✅
  cloud_firestore: ^5.5.0  # Step 6: Cloud Firestore軽量統合
  shared_preferences: ^2.2.2  # ローカル代替ストレージ

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  
  # Test utilities for Riverpod
  riverpod_test: ^0.1.9
flutter:
  uses-material-design: true

  assets:
    - lib/icon/
