// Firestore セキュリティルール（開発・テスト用）
// 本番環境では適切なセキュリティルールを設定してください

rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // 開発・テスト用: 全てのコレクションに読み書きを許可
    // 警告: 本番環境では使用しないでください
    match /{document=**} {
      allow read, write: if true;
    }
    
    // より制限的なルール例（本番用）:
    // match /location_history/{document} {
    //   allow read, write: if request.auth != null;
    // }
    // 
    // match /connection_test/{document} {
    //   allow read, write: if true;
    // }
  }
}